const appConfig = {
    apiPrefix: '/api',
    authenticatedEntryPath: '/people/accounts',
    unAuthenticatedEntryPath: '/sign-in',
    tourPath: '/',
    locale: 'en',
    enableMock: true,
}

export default appConfig
